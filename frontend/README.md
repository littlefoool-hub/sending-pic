# Image Uploader Frontend

Фронтенд приложение для загрузки изображений на React + Vite + TypeScript.

## 📋 Описание

Веб-приложение для загрузки изображений с поддержкой drag-and-drop. После загрузки пользователь получает ссылку на изображение, которую можно скопировать в буфер обмена.

## 🛠 Технологии

- **React 18** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **React Router** - маршрутизация в приложении
- **CSS Modules** - изолированные стили для компонентов
- **clsx** - утилита для условного объединения классов

## 📦 Требования

- Node.js версии 18 или выше
- npm или yarn

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/              # React компоненты
│   │   ├── Admin/              # Административные компоненты
│   │   │   ├── AdminUsers.tsx
│   │   │   ├── AdminUsers.module.css
│   │   │   ├── UserImages.tsx
│   │   │   └── UserImages.module.css
│   │   ├── Header/             # Шапка приложения
│   │   │   ├── Header.tsx
│   │   │   └── Header.module.css
│   │   ├── ImageUploader/      # Компонент загрузки изображений
│   │   │   ├── ImageUploader.tsx
│   │   │   └── ImageUploader.module.css
│   │   ├── Login/              # Страница входа
│   │   │   ├── Login.tsx
│   │   │   └── Login.module.css
│   │   ├── Notification/       # Компонент уведомлений
│   │   │   ├── NotificationContainer.tsx
│   │   │   └── NotificationContainer.module.css
│   │   ├── ProgressBar/        # Компонент прогресс-бара
│   │   │   ├── ProgressBar.tsx
│   │   │   └── ProgressBar.module.css
│   │   ├── Register/           # Страница регистрации
│   │   │   ├── Register.tsx
│   │   │   └── Register.module.css
│   │   ├── ProtectedRoute.tsx  # Компонент защиты роутов
│   │   └── Unauthorized/       # Страница "доступ запрещен"
│   │       ├── Unauthorized.tsx
│   │       └── Unauthorized.module.css
│   ├── contexts/               # React контексты
│   │   ├── AuthContext.tsx     # Контекст аутентификации
│   │   └── NotificationContext.tsx  # Контекст уведомлений
│   ├── services/               # Сервисы для работы с API
│   │   └── api.ts             # API функции для загрузки изображений и аутентификации
│   ├── utils/                  # Утилиты
│   │   └── validation.ts      # Валидация изображений
│   ├── App.tsx                # Главный компонент с роутингом
│   ├── App.css                # Глобальные стили приложения
│   ├── main.tsx               # Точка входа
│   └── index.css              # Базовые стили
├── nginx.conf                 # Конфигурация Nginx для production
├── Dockerfile                 # Конфигурация Docker
├── package.json               # Зависимости и скрипты
├── tsconfig.json              # Конфигурация TypeScript
├── vite.config.ts             # Конфигурация Vite
└── README.md                  # Этот файл
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd frontend
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

Vite автоматически проксирует запросы к `/api` на `http://localhost:8080` (ваш бэкенд).

### 3. Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`.

### 4. Предпросмотр production сборки

```bash
npm run preview
```

## 🔧 Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера на http://localhost:3000 |
| `npm run build` | Сборка проекта для production |
| `npm run preview` | Предпросмотр production сборки |
| `npm run lint` | Проверка кода линтером |

## 🐳 Docker

### Сборка образа

```bash
docker build -t image-uploader-frontend .
```

### Запуск контейнера

```bash
docker run -p 80:80 image-uploader-frontend
```

Приложение будет доступно на **http://localhost**

### Использование с docker-compose

Если у вас есть docker-compose файл в корне проекта:

```yaml
version: '3.8'

services:
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
```


## 🔌 API Integration

### Настройка API URL

По умолчанию приложение настроено на работу с бэкендом на `http://localhost:8080`. 

В режиме разработки Vite автоматически проксирует запросы `/api/*` на бэкенд через настройки в `vite.config.ts`.

### Формат API

**Endpoint для загрузки**: `POST /api/upload`

**Формат запроса**: 
- Content-Type: `multipart/form-data`
- Поле: `image` или `file` (зависит от вашего бэкенда)

**Ожидаемый ответ**:
```json
{
  "url": "http://localhost:8080/images/abc123.jpg"
}
```

## 🔐 Аутентификация

Приложение поддерживает два типа пользователей:
- **Обычные пользователи** - могут загружать изображения
- **Администраторы** - могут просматривать всех пользователей и их изображения

### Регистрация и вход

- Регистрация доступна на главной странице (`/`)
- При регистрации можно выбрать роль администратора
- После регистрации происходит автоматический вход
- Вход доступен на странице `/login`

### Защита роутов

- `/upload` - доступна только обычным пользователям
- `/admin/users` - доступна только администраторам
- `/admin/users/:id/images` - доступна только администраторам

## 📦 Компоненты

### ImageUploader

Главный компонент для загрузки изображений. Реализует:
- Drag-and-drop функционал
- Выбор файла через кнопку
- Валидацию файла (формат, размер)
- Превью выбранного изображения
- Отображение прогресса загрузки

**Файл**: `src/components/ImageUploader/ImageUploader.tsx`

### AdminUsers

Компонент для отображения списка всех пользователей с количеством их изображений (только для администраторов).

**Файл**: `src/components/Admin/AdminUsers.tsx`

### UserImages

Компонент для отображения всех изображений конкретного пользователя (только для администраторов).

**Файл**: `src/components/Admin/UserImages.tsx`

### ProgressBar

Компонент для отображения прогресса загрузки файла.

**Файл**: `src/components/ProgressBar/ProgressBar.tsx`

**Использование**:
```tsx
<ProgressBar progress={75} /> // Отображает 75%
```

## 🎨 Стилизация

Приложение использует **CSS Modules** для изоляции стилей компонентов.

### Пример использования:

```tsx
import styles from './Component.module.css';

<div className={styles.container}>
  <p className={styles.text}>Текст</p>
</div>
```

### Условные классы:

```tsx
import clsx from 'clsx';
import styles from './Component.module.css';

<div className={clsx(styles.container, {
  [styles.active]: isActive,
  [styles.disabled]: isDisabled
})}>
```

## 🔍 Валидация

Валидация изображений реализована в `src/utils/validation.ts`:

- **Поддерживаемые форматы**: JPEG, PNG, GIF, WebP
- **Максимальный размер**: 10MB
- **Проверка типа файла** через MIME-тип
- **Проверка размера файла**

## 🛣 Маршруты

Приложение использует React Router:

- `/` - страница регистрации
- `/login` - страница входа
- `/upload` - страница загрузки изображений (только для обычных пользователей)
- `/admin/users` - список пользователей (только для админов)
- `/admin/users/:id/images` - изображения конкретного пользователя (только для админов)
- `/unauthorized` - страница "доступ запрещен"

## ⚙️ Конфигурация

### Vite (vite.config.ts)

- Порт dev-сервера: `3000`
- Прокси для API: `/api` → `http://localhost:8080`

### TypeScript (tsconfig.json)

- Строгий режим включен
- JSX: `react-jsx`
- Target: `ES2020`

## 🐛 Решение проблем

### Ошибка: "Cannot find module"

```bash
# Удалите node_modules и переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Порт уже занят

Измените порт в `vite.config.ts`:

```typescript
server: {
  port: 3001, // или другой свободный порт
}
```

### Ошибки TypeScript

Проверьте, что все типы правильно импортированы. Убедитесь, что файлы `.d.ts` на месте.

### Проблемы с CORS

Убедитесь, что бэкенд настроен для работы с фронтендом. В dev-режиме Vite проксирует запросы автоматически.

## 📚 Полезные ссылки

- [React Documentation](https://react.dev)
- [TypeScript Documentation](https://www.typescriptlang.org)
- [Vite Documentation](https://vitejs.dev)
- [React Router Documentation](https://reactrouter.com)

## 📄 Лицензия

Учебный проект

## 🎯 Быстрый старт

1. Установите зависимости: `npm install`
2. Запустите dev-сервер: `npm run dev`
3. Откройте браузер: http://localhost:3000
4. Зарегистрируйтесь или войдите в систему
5. Начните загружать изображения!

---

**Удачи в разработке! 🚀**
